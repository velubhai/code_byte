 #include <stdio.h>
int main()
{
	int n, i,sum=0;
	int count=0;
    int array[n];
    printf("Enter the values:");
    for(i=0;!(n==-1);i++){
       printf("array[%d]:",i);
       scanf("%d", &n);
       sum = sum + n;
        
    }
 

 
    printf("sum of tottal nubers: %d\n",sum+1);
    return 0;
}
