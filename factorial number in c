#include <stdio.h>
int main()
{
     
    int n ;
    int i,f=1;
    printf("enter the number:") ;
   	scanf("%d",&n);
    
    
	 for(i=1;i<=n;i++)
	 f=f*i;
	 
     
    // printing the result
    printf("factorial = %d", f);
    return 0;
}
