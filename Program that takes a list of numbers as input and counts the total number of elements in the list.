#include <stdio.h>
int main()
{
	int n, i;
	int count=0;
    int array[n];
    
    for(i=0;!(n==-1);i++){
       printf("array[%d]:",i);
       scanf("%d", &n);
       array[i]=n;
       count++;
    }
 

 
    printf("Size of the  array is %d\n", count-1);
    return 0;
}
